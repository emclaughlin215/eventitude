import StyleSheet from 'react-native';

const styles = StyleSheet.create({
  headerText: {
    fontSize: 25,
  },
  menuProvider: {
    marginBottom: 10,
    marginTop: 10,
  },
});

export default styles;
